#include <iostream>
#include <cstring>
#include <algorithm>
using namespace std;
const int N = 1e5 + 10;
int n, m;
int a[N];
int f[N][20];
int main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    // st表预处理

    for (int i = 1; i <= m; i++)
    {
        int l, r;
        cin >> l >> r;

        // 询问？
    }
    return 0;
}